name: wompi_web_checkout
description: "A Dart package for Wompi's web checkout."
version: 1.0.0
homepage:

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  very_good_analysis: ^7.0.0
